package tests

import (
	"testing"
)

func TestNewSession(t *testing.T) {
	/*s := session.New()
	s.Set("test", TestStr{"Ahoy"})

	ts := s.Get("test")
	fmt.Printf("%#v", ts.(TestStr).TestField)

	s.Drop()*/
}

func TestOpenSession(t *testing.T) {
	/*s, err := session.Open("123")
	fmt.Println(s, err)*/
}
